version: 2

aliases:
  - &restore-cache
    restore_cache:
      key: dependency-cache-{{ checksum "package.json" }}
  - &install-deps
    run:
      name: Install dependencies
      command: npm ci
  - &build-packages
    run:
      name: Build
      command: npm run build && npm run bootstrap
  - &run-unit-tests
    run:
      name: Test
      command: npm run test

unit-tests-template: &unit-tests-template
  steps:
    - checkout
    - *restore-cache
    - *install-deps
    - *build-packages
    - *run-unit-tests

jobs:
  build:
    docker:
      - image: circleci/node:18
    steps:
      - checkout
      - run:
          name: Update NPM version
          command: 'sudo npm install -g npm@^8'
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: Install dependencies
          command: npm ci
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules

  test_node_lts:
    <<: *unit-tests-template
    docker:
      - image: circleci/node:18

  test_node_16:
    <<: *unit-tests-template
    docker:
      - image: circleci/node:16

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
      - test_node_16:
          requires:
            - build
      - test_node_lts:
          requires:
            - build
